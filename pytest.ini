[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
asyncio_default_test_loop_scope = session
markers =
    integration: marks tests that require live Postgres (deselect with '-m "not integration"')
addopts = -m "not integration" -v
